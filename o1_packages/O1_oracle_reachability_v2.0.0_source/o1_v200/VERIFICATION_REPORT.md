# O1 v2.0.0 verification report

**Status:** package verified; no O1 calibration or confirmation performed.

## v2 semantic migration

The primary axis order is now:

1. `A1_READABLE`
2. `A2_WRITABLE`
3. `A3_CAUSAL_MEAN`
4. `A4_SEQUENCE_MEAN`

The v1 shared-PC actuator construction and `>=0.90` shared-PC gate have been
removed from v2. They are replaced by structural gates that:

- embed the two exact `[8,2048]` float32 source matrices;
- bind the unchanged ordered eight-task reference JSONL;
- recompute each uncentered float64 arithmetic mean;
- normalize only after the mean;
- reject centering, PC extraction, source mutation, and reference reordering;
- keep the two actuator means as distinct axes.

The inadequate original d2 and the separate `D2_OUTCOME_AXIS_V2` diagnosis are
not primary-bank inputs.

All O1 chronology, CRN, K=8, zero-alpha, transport-diagnostic, calibration,
exact-power, budget, provenance, external-commitment, attempt-ledger, and
confirmatory analysis gates remain.

## Verification result

```text
53/53 confirmatory and record-integrity fixtures passed
17/17 calibration and calibration-precommit fixtures passed
15/15 v2.0 cross-artifact, chronology, package-integrity, bytecode, frozen-generator, and boundary fixtures passed
7/7 end-to-end CLI workflow fixtures passed
15/15 axis-verifier synthetic and adversarial checks passed
Exact McNemar planning minima: 324 / 817 / 2238 / 337
Python syntax compilation: 15 modules passed
JSON templates and fixed policy fields: passed
CLI parsers: 9 passed
No packaged or test-generated bytecode: passed
```

`SHA256SUMS` covers every regular package file except itself. Verification is
performed before module import.

## Assurance boundary

Synthetic fixtures verify the executable gates and adversarial rejection
behavior. A real axis package can structurally prove its A1/A2 identity to
embedded frozen source-axis bytes and its A3/A4 construction from embedded
source matrices. Upstream tap fitting, exact-protocol reconstruction, and live
adapter capture remain hash-bound or attested unless their full execution
inputs are embedded.

This report contains no empirical O1 calibration or confirmatory evidence.
